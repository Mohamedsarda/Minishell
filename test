# bash-3.2$ echo -n
# bash-3.2$ echo -n -n 
# bash-3.2$ echo -n -n  hdjf
# hdjfbash-3.2$ echo -n -an hdjf
# -an hdjfbash-3.2$ echo -n -naaa hdjf
# -naaa hdjfbash-3.2$ echo -n -n hdjf -n
# hdjf -nbash-3.2$ echo -n "-n       a" hdjf -n
# -n       a hdjf -nbash-3.2$ echo -n -n       a hdjf -n
# a hdjf -nbash-3.2$ echo -n -----n       a hdjf -n
# -----n a hdjf -nbash-3.2$ echo -n -nnnnnnnn       a hdjf -n
# a hdjf -nbash-3.2$ echo -n -nnnnnnnna       a hdjf -n
# -nnnnnnnna a hdjf -nbash-3.2$ echo ""

# bash-3.2$ echo "-"
# -
# bash-3.2$ echo "-a"
# -a
# bash-3.2$ echo "-n"
# bash-3.2$ echo "-na"
# -na
# bash-3.2$ echo "-n fj"
-n fj
# bash-3.2$ echo "-n" fhkjsd
# fhkjsdbash-3.2$ echo "-n " fhkjsd
# -n  fhkjsd
# bash-3.2$ echo "-n" -nfhkjsd
# -nfhkjsdbash-3.2$ echo "-nnnnn" -nfhkjsd
# -nfhkjsdbash-3.2$ echo "-nnn""nn" -nfhkjsd 
# -nfhkjsdbash-3.2$ echo "-nnn" "nn" -nfhkjsd 
# nn -nfhkjsdbash-3.2$ echo "-nnn""nn" -nfhkjsd 
# -nfhkjsdba
# bash-3.2$ echo -n -n"n" -n sk
# skbash-3.2$ echo -n -n"n"a -n sk
# -nna -n skbash-3.2$ echo -n -n"n" a -n sk
# a -n skbash-3.2$ echo -n -n"n"  -n sk
# skbash-3.2$ echo $HOME
# /Users/msarda

# |d
# echo -nMnnnn -n h
#  echo "$'cdhs"



# //done

#  -----


#  Minishell$ echo "'cdhs"
# 'cdhs
# Minishell$ echo "$'cdhs" // done
# 'cdhs
# Minishell$ echo "$"cdhs"
# Minishell : unexpected EOF while looking for matching `"'
# Minishell$ echo "$"cdhs""
# $cdhs
# Minishell$ echo "$'cdhs"
# 'cdhs
# Minishell$ echo "$'cdhs"$"USER"
# 'cdhs$USER
# Minishell$ echo cda
# cda
# Minishell$ echo $USER$PATH'$PATH'UYS
# msarda/Users/msarda/.brew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/munki:/Library/Apple/usr/bin:/Users/msarda/.brew/bin$PATHUYS
# Minishell$ echo $""HOME
# $HOME
# Minishell$ echo $''HOME
# ''HOME
# Minishell$ echo ''$HOME // done
# Minishell$ echo -$nnn
# -
# Minishell$ echo "'$'"
# ''
# Minishell$ echo "'$HOME'"
# '/Users/msarda'
# Minishell$ echo '$HOME'
# $HOME
# Minishell$ echo '"$HOME"'
# "$HOME"
# Minishell$ echo ''$HOME
# ''/Users/msarda
# Minishell$ echo $HOME''
# /Users/msarda''
# Minishell$ echo ""$HOME
# /Users/msarda
# Minishell$ echo $HOME""
# /Users/msarda
# Minishell$ echo $HOM''E
# ''E
# Minishell$ echo $HOM-E
# -E
# Minishell$ echo $HOM√E
# xf�
# Minishell$ echo $HOME√
# /Users/msard�xf
# Minishell$ echo $√
# xf
# Minishell$ echo $g√
# xf
# Minishell$ echo ----
# ----
# Minishell$ echo "-nnnn "
# -nnnn 
# Minishell$ echo  "   -nnnn"
#    -nnnn
# Minishell$ echo  "-nnnn"
# Minishell$ echo  "-nnnn" "    "
#     Minishell$ echo  "-nnnn" ""
# Minishell$ echo  ""

# Minishell$ echo "-nn-"
# Minishell$ echo -n $g $g $g $HOME
----

# Minishell$ export $a='$a'
# Minishell$ echo $a

# Minishell$ echo $a
# Minishell$ export a='$a'
# Minishell$ echo $a
# $a
# Minishell$ export a="$a"
# Minishell$ echo $a
# =$a
# Minishell$ echo $a
# =$a
# Minishell$ echo $a
# =$a
# Minishell$ export a="coco"
# Minishell$ echo $a
# =coco
# Minishell$ clear
# TERM environment variable not set.
# Minishell$ clear
# TERM environment variable not set.
# Minishell$ export _coco=coco
# Minishell$ export 0coco=coco

# ///
# done
# - export 0coco=coco
# - export $a='$a'

# - export a="$a"
# Minishell$ echo $a
# =$a
# exit
# run
//


# //
# Minishell$ unset HOME
# Minishell$ cd
# [1]    49238 segmentation fault  ./minishell


# ls > $dsds
# bash: $dsds: ambiguous redirect



# //

# bash-3.2$ export "" test=a
# bash: export: `': not a valid identifier
# bash-3.2$ echo $test
# a
# bash-3.2$ 


# ----------------------------

# Minishell$ export "" test=a
# Minishell$ export: `': not a valid identifier
# Minishell$ echo $test

# Minishell$ 

# ///
# bash-3.2$ > $A
# bash: $A: ambiguous redirect
# bash-3.2$ export a="a b"
# bash-3.2$ > $a
# bash: $a: ambiguous redirect


# Minishell$ export A="ls 4"
# Minishell$ export b=$A
# Minishell$ syntax error near unexpected token
# Minishell$ export b=ls 4


# -a=1
# Minishell$ export -a=1

# bash-3.2$ unset HOME
# bash-3.2$ cd
# bash: cd: HOME not set
# bash-3.2$ cd Parsing/



# exit -1     ==== -1 + 256
# exit 257     ==== 257 % 256



# bash-3.2$ > $gdfgdf
# bash: $gdfgdf: ambiguous redirect

# bash-3.2$ export a="a b"
# bash-3.2$ > $a
# bash: $a: ambiguous redirect



# handle ft_handle_herd in syntex error
`


# Minishell$ << a cat
# > $PWD''"$PWD"$$USER
# > a
# $PWD''"$PWD"$$USER


# bash-3.2$ export a="ls -la"
# bash-3.2$ $a' -la'
# ls: illegal option --  
# usage: ls [-@ABCFGHLOPRSTUWabcdefghiklmnopqrstuwx1%] [file ...]

# Minishell$ unset PATH
# Minishell$ ls
# Execution       Makefile        leaks.sh        main.o          minishell.h
# Ft_libft        Parsing         main.c          minishell       test
# // if (ft_strcmp("_", tmp->key) != 0)
# 		// 	return ;
# bad


ps aux | grep "root" | wc -l


------------------------------

<< a << b << c cat | ls
> a
> 
> ctr+c

shelvl
ECHO puercase




Minishell$ / : No such file or directory
Minishell$ ls
Execution       Parsing         leaks.sh        minishell
Ft_libft        a               main.c          minishell.h
Makefile        fd_leaks.sh     main.o          test
Minishell$ 
.git/        Parsing/     main.c       test
Execution/   a            main.o       
Ft_libft/    fd_leaks.sh  minishell    
Makefile     leaks.sh     minishell.h  
Minishell$ 
.git/        Parsing/     main.c       test
Execution/   a            main.o       
Ft_libft/    fd_leaks.sh  minishell    
Makefile     leaks.sh     minishell.h  
Minishell$ ./Parsing/
Minishell$ ./Parsing/ : No such file or directory


Minishell$ |    
Minishell$ ||
Minishell$ |||||
Minishell$ ls |
Minishell$ | ls |
//done



# Minishell$ ls
# Execution       Parsing         leaks.sh        minishell
# Ft_libft        a               main.c          minishell.h
# Makefile        fd_leaks.sh     main.o          test
# Minishell$ pwd
# /Users/eel-ghal/Desktop/Minishell
# Minishell$ env
# PWD=/Users/eel-ghal/Desktop/Minishell
# SHLVL=1
# _=/usr/bin/env
# Minishell$ ls
# Execution       Parsing         leaks.sh        minishell
# Ft_libft        a               main.c          minishell.h
# Makefile        fd_leaks.sh     main.o          test
# Minishell$ pwd
# /Users/eel-ghal/Desktop/Minishell
# Minishell$ exprot PATH=ZZ
# Minishell$ exprot: command not found
# Minishell$ expORt PATH=ZZ
# Minishell$ expORt: command not found
# Minishell$ export PATH=ZZ
# Minishell$ ls
# Minishell$ ls: command not found
# Minishell$ unset PATH
# Minishell$ LS
# Minishell$ : No such file or directory
# Minishell$ ls
# Minishell$ : No such file or directory


# Minishell$ export ++=PATH
# Minishell$ export: `++=PATH': not a valid identifier

#     1 (16 bytes) ROOT LEAK: 0x7fee05e04620 [16]  length: 1  "+" 
# //done


Minishell$ << zz
> Minishell$ %   


Minishell$ << a << b << c cat | ls
> 
> 
> 
Minishell$ ls
: command not found
Minishell$ 

# Minishell$ 
# Minishell$ $a
# ➜  Minishell
//done



Minishell$ unset ?
Minishell$ unset: `?': not a valid identifier
# Minishell$ export ?
# Minishell$ export: `?': not a valid identifier
# Minishell$ 

# bash-3.2$ export ?="ls -la"
# bash: export: `?=ls -la': not a valid identifier
# bash-3.2$ export ?
# bash-3.2$ export ?=1
# bash: export: `?=1': not a valid identifier
# bash-3.2$ export ?=fa
# bash: export: `?=fa': not a valid identifier


# export cmd=" -la "
# ls > $cmd
# not ab
# Minishell$ ls > ''
# Minishell : ambiguous redirect
# ls >>> f
# | ls |  >f
# ls | d | << f |

# Minishell$ ls > $das | ls > $dsa
# Minishell : ambiguous redirect
# Minishell$ 


{
    # Minishell$ ls | ls < a | << a
    # Minishell$ : a: No such file or directory
    # > 

    # bash-3.2$ ls | ls < a | << a
    # > 
}







bash-3.2$ ls < das | ls < fadf
bash: das: No such file or directory
bash: fadf: No such file or directory


bash-3.2$ ls < dsa | < > | ks < dasf
bash: syntax error near unexpected token `>'

Minishell$ ls | | 
Minishell$ asdas :syntax error near unexpected token


bash-3.2$  ls  < qwe > $kfldf | > asd
bash: qwe: No such file or directory
bash-3.2$ 


<< '$HOME'$""$?

run minishell test cat - | grep m
qe
m
m
ctrl + c
echo $?
1
echo $?
1
echo $?
1
the exit status should be 130 and it should rest to 0 after the second echo
